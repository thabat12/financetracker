[pytest]
asyncio_default_fixture_loop_scope = session

# You can also add other configuration options as needed
# For example, to specify the directories to search for tests
testpaths = tests

# To enable warnings, you can add:
filterwarnings = ignore::UserWarning

# You can add any other pytest-related settings you want to configure
